{
  "name": "pintasso-lambda",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "esbuild sendEmail.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "package": "pnpm build && cd dist && zip lambda.zip index.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/nodemailer": "^6.4.16",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
